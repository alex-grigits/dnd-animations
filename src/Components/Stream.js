// Core
import React from 'react';

// Components
import './Stream.css';

const Stream = (props) => {
    return (
        <div className = 'stream'>
            Stream
        </div>
    );
};

export { Stream };
